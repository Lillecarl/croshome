You've now stopped {{ count }} {{ "time" if count == 1 else "times" }} in a row without calling need_user. If you've finished everything you can do autonomously, call need_user. Otherwise keep going. If you are stopping because you typed something into this session that needs your turn to end before it runs, that is what the send tool's `resume` argument is for -- send it again with `resume` set to what you want typed back to you, then stop. If instead you are waiting on something outside this session -- a build, a CI run, a file another process writes -- start `wrapty-monitor start --until CMD --wake TEXT` and then stop: it wakes you when the condition holds, and it permits you to stop for as long as it runs.{% if can_compact %} Context usage is {{ "%.0f"|format(used_pct) }}%. If this is a good point to compact before you start the next thing, you may call the compact tool now.{% endif %}

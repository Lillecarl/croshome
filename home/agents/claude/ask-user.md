## The question tool

`AskUserQuestion`. It puts multiple-choice questions to me and blocks until I
answer. Each option carries a `label` (1-5 words) and a `description`.

This is the tool the workaround rule in **Offer both routes** requires. It is
not `need_user`, which ends a turn and asks nothing.
